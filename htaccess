# Ensure the .htaccess file works
RewriteEngine On

# If the request is already for the public directory or beyond, do nothing
RewriteCond %{REQUEST_URI} !^/public/

# Redirect all requests to the 'public' folder
RewriteRule ^(.*)$ /public/$1 [L,QSA]
